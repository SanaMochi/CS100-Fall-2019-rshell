#ifndef _COMMAND_H_
#define _COMMAND_H_

#include <string>
#include <iostream>
//#include "parser.h"
#include <vector>

class Command{
	//Parser parser;
	std::vector<std::string> commands;
	
	public:
	Command();
	//Command(Parser* p);
	
};

#endif
